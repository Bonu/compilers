// if_whileE: Check statement types: new array, assignments, and array use.
class Main {

  void main() {

    int A[5];     int B[5];
    int i;        int j;
    bool p;       bool q;
    float f;

    A = B;
    i = p;
    while ( i) {
      A[i] = B[i]+0.5;
	{
	   int k;
	   bool f;
	   j = 0;
           while ((j < 10) && i) {
               A[i] = 0.3;
	       j = j + 1;
	   }
           f = k%(k*2);
           a = a + 1;
	}
	   if (p == q) p = q;
	   if (i+j >= A[j*.2])
	       i = j+10.1;
	   else {
	       p = p && f;
	       i = i%p;
           }
    }

	while ((i > 0) && (j == i)) {
		i = (i>0) || (j>0);
	}

  }
  int m(int b[]) {
	char ch[5];
	float y = x[0];
	char ch0 = z;
  }

  float x = 0.0;

}
