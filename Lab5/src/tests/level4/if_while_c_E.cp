// if_whileE: Check statement types: new array, assignments, and array use.
class Main {

  void main() {

    char A[5];    char B[5];    char i;    char j;
    bool p;       bool q;
	int f;

	A = B;

    i = p;
    while ( i) {
      A[i] = B[i]+5;
	  {
	     char k;
	     int f;
	 j = 0;
         while ((j < 10) && i) {
            A[i] = 0 + '.' + 3;
	    j = j + 1;
	 }
         f = k%(k*2);
      a = a + 1;
    }
	  if (p == q) p = q;
	  if (i+j >= A[j*2])
	     i = j+10+1;
	  else {
		 p = p&&q;
	     i = i%p;
        }
    }

	while ((i > 0) && (j == i)) {
		i = (i>0) || (j>0);
	}

  }
}
