
// Main: Test logical operators.

class Main{

    int i = 5;
    float a = 3.2E+5;
    bool p = true;

  /* Simple Logical Expressions */
    bool q = a && p;
    bool r = p && i;

    bool s = !i;
    bool t = !a;

    bool u = p || a;
    bool v = i || a;

  void main(){
  }
} /* end of Main */
