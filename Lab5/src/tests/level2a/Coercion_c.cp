// Main: Test expressions of overloaded operators.

class Main{

    char i = 'i';
    char j = 'j';
    char k = 'k';
    int a = 1;
    int b = 455;
    int c = 314159;

  // Mathematical Expressions 
   // Binary Operators 
    int d = a * b;
    int e = c / 5;
    int f = b + a;
    int aa = 10 - k;

   // Unary Operators 
    int ab = -5;
    int ac = -i;

   // Mixed & Complicated 
    int ad = i + 100 - 5*j / (k/20);
    int ae = a * 4 / '6' + (3 * i - 1);
    int af = (-a + b * '3') / (c - +b);

  void main(){
  }
}
