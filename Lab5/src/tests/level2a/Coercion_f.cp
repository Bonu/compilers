// Main: Test expressions of overloaded operators.

class Main{

    int i;
    int j;
    int k;
    float a = 1.2;
    float b = 4.5E5;
    float c = .0314159e2;

  // Mathematical Expressions 
   // Binary Operators 
    float d = a * b;
    float e = c / 5.2;
    float f = b + a;
    float aa = 10 - k;

   // Unary Operators 
    float ab = -5.6;
    float ac = -i;

   // Mixed & Complicated 
    float ad = c + 100 - 5*c / (b/2.0);
    float ae = a * 4 / 6.7 + (3 * i - 1);
    float af = (-a + b * 3.3) / (c - -b);

  void main(){
  }
}
